name: CI

on: [push, pull_request]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pyyaml
      - run: python scripts/ci/test_no_leaks.py
      - run: python scripts/ci/test_emotion.py
      - run: python scripts/ci/test_mode_switch.py
      - run: python scripts/ci/test_tone_map.py
